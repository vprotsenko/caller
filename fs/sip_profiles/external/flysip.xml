<!-- Перший транк: креди з .env через fs/vars.xml (Plan.md §3).
     Без кредів у .env gateway піднімається з заглушками і register=false —
     профіль живий, дзвінки неможливі (досить для локальних тестів).
     Керування кількома профілями з UI — етап 5 (генерація цих файлів + rescan). -->
<include>
  <gateway name="flysip">
    <param name="proxy" value="$${sip_gw_server}:$${sip_gw_port}"/>
    <param name="realm" value="$${sip_gw_server}"/>
    <param name="username" value="$${sip_gw_username}"/>
    <param name="from-user" value="$${sip_gw_username}"/>
    <param name="password" value="$${sip_gw_password}"/>
    <param name="register" value="$${sip_gw_register}"/>
    <param name="register-transport" value="udp"/>
    <param name="expire-seconds" value="600"/>
    <param name="retry-seconds" value="30"/>
    <param name="caller-id-in-from" value="true"/>
  </gateway>
</include>
