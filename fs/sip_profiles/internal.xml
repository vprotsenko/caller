<!-- Profile for registering operator softphones (MicroSIP/Zoiper) on
     5060 (stage 3). Calls to operators go via a bridge to
     user/<ext> through sofia_contact from the directory. Trimmed-down vanilla
     internal, no TLS (a POC trade-off). -->
<profile name="internal">
  <settings>
    <param name="debug" value="0"/>
    <param name="sip-trace" value="no"/>

    <param name="sip-port" value="$${internal_sip_port}"/>
    <param name="dialplan" value="XML"/>
    <param name="context" value="default"/>

    <param name="rfc2833-pt" value="101"/>
    <param name="dtmf-type" value="rfc2833"/>
    <param name="dtmf-duration" value="2000"/>

    <param name="inbound-codec-prefs" value="$${global_codec_prefs}"/>
    <param name="outbound-codec-prefs" value="$${global_codec_prefs}"/>
    <param name="inbound-codec-negotiation" value="generous"/>

    <param name="rtp-timer-name" value="soft"/>
    <param name="rtp-timeout-sec" value="300"/>

    <param name="manage-presence" value="false"/>
    <param name="nonce-ttl" value="60"/>

    <!-- operator registrations and calls — only with digest authentication
         against the directory -->
    <param name="auth-calls" value="true"/>
    <param name="challenge-realm" value="auto_from"/>
    <param name="inbound-reg-force-matching-username" value="true"/>

    <param name="rtp-ip" value="$${local_ip_v4}"/>
    <param name="sip-ip" value="$${local_ip_v4}"/>
    <param name="ext-rtp-ip" value="$${external_ip}"/>
    <param name="ext-sip-ip" value="$${external_ip}"/>
  </settings>
</profile>
