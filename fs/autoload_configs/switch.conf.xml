<configuration name="switch.conf" description="Core Configuration">
  <settings>
    <param name="colorize-console" value="true"/>
    <param name="loglevel" value="info"/>
    <param name="dump-cores" value="no"/>
    <!-- одна кампанія, max_concurrent 1..5 — стелі з великим запасом -->
    <param name="max-sessions" value="100"/>
    <param name="sessions-per-second" value="10"/>
    <param name="max-db-handles" value="50"/>
    <param name="db-handle-timeout" value="10"/>
    <!-- вузький RTP-діапазон: менше дірок у фаєрволі, вистачає на ~50 дзвінків -->
    <param name="rtp-start-port" value="16384"/>
    <param name="rtp-end-port" value="16484"/>
  </settings>
</configuration>
